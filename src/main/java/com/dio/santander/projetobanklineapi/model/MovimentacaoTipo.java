package com.dio.santander.projetobanklineapi.model;

public enum MovimentacaoTipo {
    /**
     * Enum no Java
     * São tipos de campos que consistem em um conjunto fixo de constantes (static final),
     * sendo como uma lista de valores pré-definidos.
     * Na linguagem de programação Java, pode ser definido um tipo de enumeração
     * usando a palavra chave enum.
     *
     * Na declaração é definida uma classe chamada de tipo enum.
     * O corpo da classe enum pode incluir métodos e outros campos.
     * O compilador automaticamente adiciona alguns métodos especiais quando se cria um enum.
     */

    RECEITA,
    DESPESA
}

